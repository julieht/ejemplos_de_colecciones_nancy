//ejercicio de colecciones en javascript en este caso uso de una matriz

var colors = ['red', 'green', 'blue', 'yellow']; /*se define una variable en este caso es colors y se define las valiables que a contener*/
for (var i = 0; i < colors.length; i++){ //se crea una matriz
    console.log(colors[i]);// se muestra en panatalla las variables asignadas
}